import { Layout } from "@douyinfe/semi-ui"
import { VersionInfo } from "./VersionInfo"

export const Footer = () => {
  return (
    <Layout.Footer className="mt-auto px-4">
      <VersionInfo />
    </Layout.Footer>
  )
}
