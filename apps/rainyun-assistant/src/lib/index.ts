export { platformBridge } from "./bridge"

// Usage example:
/*
import { platformBridge } from './index';

async function testSum() {
  try {
    const result = await platformBridge.sum([1, 2, 3, 4, 5]);
    console.log('Sum result:', result);
  } catch (error) {
    console.error('Error calculating sum:', error);
  }
}
*/
